{
  api {
    host: "localhost",
    host: ${?API_HOST},
    port: 8090
    port: ${?API_PORT}
  }

  db {
    host: "localhost",
    host: ${?DB_HOST},
    port: 5432,
    port: ${?DB_PORT},
    user: "sa",
    user: ${?DB_USER},
    password: "",
    password: ${?DB_PASSWORD},
    database-name: "postgres",
    url = "jdbc:h2:mem:"${db.database-name}
    driver: "org.h2.Driver",
    pool: 5,
    pool: ${?DB_POOL}
  }

  title-batch: 20
  title-batch: ${?TITLE_BATCH}
}